# P7 Stabilization and P8 Plan

## 0. 总体结论

当前阶段不要开始 `@bot`、Auto Inject、Auto Reply。  
先把手动链路稳定下来：

```text
P7 = IM -> Agent 稳定化
P8 = Agent -> IM 稳定化 + workspace 文件回传 MVP
```

注意：

```text
P8 不是从零重做 terminal output -> IM draft。
这部分早期已经有 demo：Selection -> IM、Capture Output、Output -> Draft、Draft -> Chat。
P8 的任务是把这些已有 demo 能力改名、分组、固定边界，并新增 workspace file -> IM attachment。
```

---

# P7 Stabilization: IM -> Agent

## 1. P7 定位

P7 的核心是：

```text
OpenIM messages / attachments
  -> IMContextService
  -> context markdown
  -> manifest
  -> workspace attachments
  -> terminal / agent runtime reads by path
```

也就是把 IM 的上下文稳定交给右侧 terminal / agent。

P7 当前已经有 MVP：

```text
- selected / recent / history / search context bundle
- context markdown
- manifest
- attachment extraction
- workspace attachment export
- path prompt
- context history record
```

但真实使用中暴露出 UX 和语义问题，所以 P7 现在进入稳定化。

---

## 2. P7 非目标

P7 稳定阶段不做：

```text
1. 不做 @bot。
2. 不做 Auto Inject。
3. 不做 Auto Reply。
4. 不做 Agent -> IM 自动回复。
5. 不做 workspace file -> IM attachment send。
6. 不做 terminal output path 自动解析。
7. 不做 runtime-specific multimodal adapter。
8. 不直接调用 OpenAI / Anthropic / Gemini 多模态 API。
```

---

## 3. P7 当前主要问题

### 3.1 Context Window 过重

现在用户从 IM 选中消息后发送到 terminal，会弹出一个很大的 context 窗口。  
这个窗口混合了：

```text
- recent context
- selected context
- preview
- context history
- copy prompt
- send prompt
- markdown / manifest 操作
```

这让用户感觉 IM -> terminal 中间多了一层不必要的面板。

### 3.2 Send Prompt 语义混乱

当前不同位置都有 `Send` / `Send Prompt`，但含义不同：

```text
IM selected toolbar:
  应该发送当前选中的消息 context。

Terminal 顶部 Send Prompt:
  实际发送 lastContextPrompt，不一定是当前选中消息。

Context Window footer Send Prompt:
  可能重新创建 recent context，而不是发送当前 previewed context。

Context History record Send:
  发送某条历史 record 的 prompt。
```

这些入口名称太接近，导致 selected / recent / last context 容易混淆。

### 3.3 Context History 意义不清

Context History 本身有意义，但不应该作为 IM -> terminal 的默认中间层。

它应该是：

```text
Context Library / Context History / Debug Panel
```

用于查看、复用、调试 context bundle，而不是每次 selected send 都弹出来。

---

# P7.1 Context Window / Handoff UX Cleanup

## 4. 目标交互

用户选中 N 条消息，点击：

```text
More -> Agent -> Send Selected to Terminal
```

应该直接执行：

```text
1. 根据本次 selected messageIDs 创建 selectedMessages bundle。
2. 写入 workspace/context/*.md 和 *.manifest.json。
3. 直接发送这个 selected bundle 的 prompt 到 active terminal。
4. toast: Sent selected context to terminal: N messages。
5. 不打开 Context Window。
```

只有用户点击：

```text
Preview Selected Context
```

才打开 Context Window。

---

## 5. IM_CONTEXT_ACTION 规则

修改 TerminalDock 的 `handleContextAction` 逻辑。

不能再无条件：

```ts
setContextModalOpen(true)
```

应改成：

```ts
if (params.action === "preview") {
  setContextModalOpen(true);
  createMessagesContext({}, params);
  return;
}

if (params.action === "copy") {
  createMessagesContext({ copyPrompt: true }, params);
  return;
}

if (params.action === "send") {
  createMessagesContext({ sendPrompt: true }, params);
  return;
}
```

要求：

```text
preview:
  打开 Context Window。

copy:
  不打开 Context Window，直接复制本次 context prompt。

send:
  不打开 Context Window，直接发送本次 context prompt 到 terminal。
```

---

## 6. Context Window 重命名和重新定位

当前窗口名：

```text
Create Context
```

建议改为：

```text
Context Library
```

或：

```text
Context History
```

定位：

```text
高级上下文库 / 历史 / 调试 / 复用面板
```

用于：

```text
1. 查看历史 context bundle。
2. 查看 markdown / manifest / attachment export 状态。
3. 复制某条历史 context 的 prompt / markdown / manifest / full path。
4. 重新发送某一条历史 context。
5. 打开 workspace 文件。
```

它不应该在 selected send 时自动弹出。

---

## 7. Context History 保留，但默认隐藏

Context History 的意义：

```text
1. 调试刚才生成的是 selected 还是 recent。
2. 查看 message count / attachment count。
3. 复用之前生成的 context。
4. 检查真实 OpenIM 附件导出结果。
5. 定位 manifest / markdown / attachment 文件。
```

入口建议放在：

```text
Terminal Context ▾ -> Open Context Library
```

---

## 8. Button 文案修正

### 8.1 IM selected toolbar

`More -> Agent` 建议改为：

```text
Send Selected to Terminal
Copy Selected Prompt
Preview Selected Context
Create Selected Context File
```

语义：

```text
Send Selected to Terminal:
  直接发送，不弹窗。

Copy Selected Prompt:
  直接复制，不弹窗。

Preview Selected Context:
  打开 Context Library。

Create Selected Context File:
  只写 markdown / manifest，不发送。
```

### 8.2 Terminal 顶部按钮

当前：

```text
Send Prompt
```

应改为：

```text
Send Last Context
```

或：

```text
Send Last Context Prompt
```

因为它发送的是 `lastContextPrompt`，不是当前 IM 选中消息。

### 8.3 Context History record 按钮

当前：

```text
Copy Prompt
Copy MD
Copy Manifest
Copy Full Path
Open
Send
```

建议改为：

```text
Copy This Prompt
Copy This MD
Copy This Manifest
Copy This Full Path
Open This Context
Send This Context
```

### 8.4 Context Window footer

当前底部：

```text
Close
Recent Preview
Selected Preview
Copy Prompt
Send Prompt
```

建议改为：

```text
Close
Preview Recent
Preview Selected
Copy Previewed Context
Send Previewed Context
```

要求：

```text
1. Copy Previewed Context 必须使用当前 contextPreviewBundle。
2. Send Previewed Context 必须使用当前 contextPreviewBundle。
3. 不允许 footer 里的 Copy/Send 偷偷调用 createRecentContext。
4. 没有 contextPreviewBundle 时，Copy/Send Previewed Context disabled。
```

---

## 9. Prompt 内容修正

无论 selected / recent / history / search，promptText 里都应包含 source summary：

```text
Context source: selectedMessages
Message count: 3
Attachment count: 1
Context file: context/xxx.md
Manifest file: context/xxx.manifest.json
```

这样 terminal 里能直接看出 agent 收到的是哪一类 context。

---

## 10. 可选：Inline Context Handoff

当前 path prompt 模式是：

```text
请读取 context/xxx.md
```

P7 稳定阶段可以先不强制做 inline，但建议预留：

```text
Send Inline Context
Copy Inline Context
```

它会把 `bundle.markdown` 全文直接发送到 terminal。

最低要求：

```text
先把 selected / recent / last context 的语义分清楚。
```

---

# P7.2 Real OpenIM Attachment Validation

## 11. 真实附件验证目标

P7 当前附件导出主要经过 mock/harness 验证。需要做真实 OpenIM 验证。

测试对象：

```text
1. 真实图片消息。
2. 真实文件消息。
3. 真实发送端附件。
4. 真实接收端附件。
```

需要确认：

```text
1. pictureElem 真实字段结构。
2. fileElem 真实字段结构。
3. sourcePath / filePath 在接收端是否存在。
4. sourceUrl 是否可以被 Electron main process 下载。
5. workspaceRelativePath 文件是否真实存在。
6. terminal agent 是否能根据 path 读取文件。
```

---

## 12. 最小真实测试流程

准备两个 OpenIM 用户：

```text
User A: 当前开发版 Electron 登录
User B: 另一个客户端登录
```

流程：

```text
1. B 给 A 发送一条文本消息。
2. B 给 A 发送一张图片。
3. B 给 A 发送一个普通文件。
4. A 选中这些消息。
5. A 点击 Send Selected to Terminal。
6. 检查 context markdown。
7. 检查 manifest attachments[]。
8. 检查 attachments/{bundleId}/... 是否有真实文件。
9. terminal 中执行 dir / ls / cat / file 等命令确认路径可读。
```

验收：

```text
1. selected context messageCount 正确。
2. 图片/文件 attachment 进入 manifest。
3. exported 附件对应文件真实存在。
4. failed/referenced/skipped 附件有明确状态和错误原因。
```

---

## 13. P7 附件硬化

需要补充：

```text
1. 图片优先选择有 URL 的 sourcePicture / bigPicture / snapshotPicture。
2. 不盲目信任 sourcePath / filePath。
3. 对远端消息的 local path copy 增加限制。
4. sourceUrl 下载增加 localhost / 127.0.0.1 / 内网 IP / link-local 防护，或配置 allowlist。
5. stats 增加 referencedAttachmentCount / skippedAttachmentCount。
6. markdown 默认不要暴露绝对 workspacePath。
7. 附件导出失败不能阻塞整个 context bundle。
```

---

## 14. P7 E2E 要求

Context handoff E2E：

```text
1. 选中 3 条消息 -> Send Selected to Terminal。
2. 断言 Context Window 没有打开。
3. 断言生成 selectedMessages bundle。
4. 断言 messageCount === 3。
5. 断言 terminal 收到 prompt 中包含 selectedMessages 和 3 messages。
6. 打开 Context Library。
7. 断言 history 中新增 selectedMessages record。
8. 点击某条 history record 的 Send This Context。
9. 断言发送的是该 record 的 prompt。
```

Attachment E2E：

```text
1. mock image message exported。
2. mock file message failed。
3. 断言 manifest attachments[]。
4. 断言 markdown Attachments section。
5. 断言失败附件不阻塞 context 创建。
```

---

## 15. P7 验收标准

P7 稳定完成后，应满足：

```text
1. selected messages -> terminal 一键直达，不弹 Context Window。
2. Context Window 改为 Context Library / History，只主动打开。
3. Send Selected / Send Last Context / Send This Context 语义分开。
4. Context History record 的 Send / Copy 只作用于当前 record。
5. Modal footer 不再偷偷创建 recent context。
6. Prompt 中包含 source kind / message count / attachment count。
7. 真实 OpenIM 图片/文件消息至少完成一轮手动验证。
8. 附件 manifest 状态清楚：exported / failed / referenced / skipped / unsupported。
9. lint / tsc / build / e2e 通过。
```

---

# P8 Plan: Agent -> IM Stabilization + Workspace File Handoff

## 16. P8 定位

P8 不做 @bot。  
P8 不做全自动回复。  
P8 也不是从零实现 terminal output -> IM draft。

P8 的准确定位是：

```text
已有 Agent -> IM 文本 handoff 的产品化收口
+
workspace file -> IM attachment MVP
```

P8 处理的是：

```text
Terminal / Agent output
  -> user review
  -> IM draft / IM attachment
  -> user confirms send
```

---

## 17. P8 非目标

P8 不做：

```text
1. 不做 @bot trigger。
2. 不做 Auto Inject。
3. 不做 Auto Reply。
4. 不允许远端消息自动驱动本机 terminal。
5. 不自动把 agent 输出发回 IM。
6. 不自动解析 terminal 输出里的文件路径。
7. 不自动发送 agent 生成的文件。
8. 不做多 agent 协作。
```

---

# P8.1 Existing Text Handoff Stabilization

## 18. 当前已有能力

当前已经有 demo 级能力：

```text
Selection -> IM:
  实际是 terminal selection -> IM draft/input。

Capture Output:
  实际是 terminal output -> IM draft/input。

Output -> Draft:
  实际是 auto capture terminal output -> IM draft。

Draft -> Chat:
  experimental auto-send draft to chat。
```

所以 P8 不要重复实现这些能力，而是要：

```text
1. 改名。
2. 分组。
3. 固定默认行为。
4. 明确哪些只写 draft。
5. 明确哪些会发送。
6. 补 E2E 防止后续改坏。
```

---

## 19. Button 改名

```text
Selection -> IM
  改为 Selection -> Draft

Capture Output
  改为 Capture Output -> Draft

Output -> Draft
  改为 Auto Capture Output -> Draft

Draft -> Chat
  保留 Draft -> Chat (experimental)
```

语义：

```text
Selection -> Draft:
  只把 terminal 选中文本追加到 IM 输入框，不发送。

Capture Output -> Draft:
  只把 terminal 当前输出写入 IM 输入框，不发送。

Auto Capture Output -> Draft:
  自动把 terminal 输出同步到 IM 输入框，默认关闭。

Draft -> Chat:
  真正发送 IM 消息，默认关闭，experimental。
```

---

## 20. Terminal 区域按方向分组

当前 terminal 区域按钮混在一起：

```text
Run
Start
Context
Send Prompt
Selection -> IM
Capture Output
Output -> Draft
Draft -> Chat
Clear
```

需要按方向分组。

### Runtime controls

```text
Run
Start / Stop
Interrupt
Restart
Clear
```

### IM -> Agent

```text
Send Selected Context
Send Last Context
Open Context Library
Create Recent Context
```

### Agent -> IM

```text
Selection -> Draft
Capture Output -> Draft
Auto Capture Output -> Draft
Draft -> Chat experimental
Attach Workspace File
```

核心原则：

```text
IM -> Agent:
  把 IM 上下文送进 terminal/agent。

Agent -> IM:
  把 terminal/agent 输出带回 IM draft 或附件发送区。
```

---

## 21. 默认策略

```text
Selection -> Draft:
  手动动作，可用。

Capture Output -> Draft:
  手动动作，可用。

Auto Capture Output -> Draft:
  默认关闭。

Draft -> Chat:
  默认关闭，experimental，开启时必须确认。
```

不能默认把 terminal 输出持续塞进 IM 输入框，也不能默认自动发送到聊天。

---

# P8.2 Text Handoff E2E

## 22. Text handoff 测试

```text
1. terminal 有 mock output。
2. 用户点击 Capture Output -> Draft。
3. 断言 IM draft 被替换为 captured output。
4. 不自动发送 chat message。
```

## 23. Selection handoff 测试

```text
1. terminal 有 selected text。
2. 点击 Selection -> Draft。
3. 断言 IM draft 追加 selected text。
4. 不自动发送。
```

## 24. Auto capture 测试

```text
1. Auto Capture Output -> Draft 默认关闭。
2. 开启后 terminal output 更新 draft。
3. 关闭后不再更新。
```

## 25. Draft -> Chat 测试

```text
1. 默认关闭。
2. 开启时需要 experimental warning。
3. e2e 只验证事件触发，不做真实发送。
```

---

# P8.3 Workspace File -> IM Attachment MVP

## 26. 新增能力

P8 真正新增的重点是：

```text
workspace file -> IM attachment
```

目标：

```text
agent 在 workspace 里生成文件
  -> 用户点击 Attach Workspace File
  -> 选择文件
  -> 预览文件名 / 大小 / 类型
  -> 用户确认
  -> 作为 IM 附件发送或加入待发送区
```

第一版支持：

```text
1. 图片
2. 普通文件
```

---

## 27. Attach Workspace File 入口

入口建议放在：

```text
Terminal -> Agent -> IM group -> Attach Workspace File
```

或：

```text
Agent -> IM ▾ -> Attach Workspace File
```

流程：

```text
1. 打开当前 conversation workspace。
2. 用户选择文件。
3. 显示确认弹窗：
   - file name
   - size
   - type
   - relative path
4. 用户点击 Attach / Send。
5. 根据类型走 OpenIM 图片/文件消息发送链路。
```

如果当前项目还没有现成文件发送能力，可先做：

```text
Attach to Draft / Pending Attachment
```

不直接发送。

---

## 28. P8 文件回传安全规则

```text
1. 不执行 workspace 文件。
2. 不自动发送 workspace 文件。
3. 不自动解析 terminal output path。
4. 发送前必须用户确认。
5. 文件大小限制。
6. 文件名 sanitize。
7. 不允许 workspace 外路径，除非用户通过文件选择器显式选择。
8. Draft -> Chat 必须保留 experimental 提示。
```

---

## 29. P8 Attach Workspace File E2E

第一版可以 mock：

```text
1. mock workspace file list。
2. 用户点击 Attach Workspace File。
3. 选择一个 file。
4. 弹确认。
5. 确认后进入 pending attachment / draft。
6. 不自动发送。
```

如果已有真实 OpenIM 文件发送能力，则增加 optional manual test：

```text
1. agent workspace 中存在 result.png。
2. 用户 Attach Workspace File。
3. 选择 result.png。
4. 确认发送。
5. IM 会话出现图片消息。
```

---

## 30. P8 验收标准

P8 完成后应满足：

```text
1. Terminal 按钮按方向分组。
2. IM -> Agent 和 Agent -> IM 不再混在一起。
3. 已有 Selection/Capture/Draft 文本 handoff 被正确改名和固定边界。
4. Selection -> Draft 只写入 IM draft，不发送。
5. Capture Output -> Draft 只写入 IM draft，不发送。
6. Auto Capture Output -> Draft 默认关闭。
7. Draft -> Chat 默认关闭，仍然 experimental。
8. Attach Workspace File 有手动确认。
9. 不做 @bot。
10. 不做 Auto Reply。
11. lint / tsc / build / e2e 通过。
```

---

# 31. 阶段完成后的状态

P7 稳定后：

```text
IM selected messages / attachments
  -> stable context bundle
  -> direct terminal handoff
  -> context history becomes library/debug panel
```

P8 完成后：

```text
Terminal / Agent output
  -> user-reviewed IM draft
  -> optional manually confirmed file attachment handoff
```

仍然不进入自动 bot。

后续阶段另开：

```text
P9: @bot Trigger Detection only
P10: Pending Prompt + Manual Inject
P11: Optional Auto Inject
P12: Full Auto Reply Experiment
```
